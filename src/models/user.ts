export class User {

    /**
     * コンストラクタ
     * @param id ID
     * @param fullname フルネーム
     * @param department 部署
     * @param _id ID
     */
    constructor(
        id: string,
        fullname: string,
        department: string,
        _id: string) {
    }
}